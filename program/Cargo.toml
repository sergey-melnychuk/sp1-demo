[package]
name = "sp1-https-json-program"
version = "0.1.0"
edition = "2021"

[dependencies]
sp1-zkvm = "6.0.0"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", default-features = false, features = ["alloc"] }
